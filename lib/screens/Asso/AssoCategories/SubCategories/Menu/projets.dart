import 'package:flutter/material.dart';

class Projets extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Ceci est l'onglet Projets"),
    );
  }
}
